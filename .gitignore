# Python
.venv/
venv/
ENV/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Node / Frontend
frontend/node_modules/
frontend/.vite/
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs (keep dist tracked or let Render build it)
# We can include frontend/dist so Render doesn't even need node if you just want to run python
# But if render builds it, we can keep dist clean

# System & OS
.DS_Store
Thumbs.db
*.log
